{
  "name": "serverless-aws-step-functions-progress-tracking",
  "version": "1.0.0",
  "description": "step function orchestration for reactive progress updates using web sockets",
  "main": "handler.js",
  "scripts": {
    "deploy": "npx serverless deploy",
    "deploy-ffmpeg-layers": "cd layers && cd ffmpeg && npm run build && npm run development-deploy"
  },
  "keywords": [],
  "author": "HTLuff",
  "license": "ISC",
  "dependencies": {
    "aws-sdk": "^2.1510.0",
    "serverless-api-gateway-throttling": "^2.0.3",
    "serverless-iam-roles-per-function": "^3.2.0",
    "serverless-step-functions": "^3.17.0"
  }
}
